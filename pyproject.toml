[project]
name = "stuttering-classification-api"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "fastapi[standard]",
    "python-multipart",
    "gradio_client",
    "beautifulsoup4"
]

[tool.fastapi]
entrypoint = "main:app"